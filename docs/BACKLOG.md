# Backlog — Michaelin Map

> The single home for pending items: technical debt, scope cuts, source-material divergences and open decisions.
> Read at boot alongside `docs/STATUS.md`.

---

## 1. F-01 technical corrections — ✅ all closed (S04)

Found in the audit of the original schema (`docs/files/2026-08-05-supabase-schema.sql`), applied in the
migration `20260806120000_f01_schema_rls_rpc.sql` and verified by the inline gates.

| # | Finding | Severity | How it ended up |
|---|---|---|---|
| ✅ BL-01 | `codes` with public `SELECT` let any visitor list every secret code | 🔴 High | `codes` has no SELECT policy at all, and `anon` does not even have a GRANT on the table. Only `rpc_redeem_code()`, which answers with an identical `{"ok": false}` on every failure so it cannot become an oracle |
| ✅ BL-02 | `field_reports` with `insert with check (true)` let a visitor write `status = 'published'` directly | 🔴 High | No INSERT policy exists on the table. Only `rpc_submit_field_report()`, which derives the status from `questions.requires_review`. The column default became `pending` (fail closed) |
| ✅ BL-03 | `auth.role() = 'authenticated'` gave full write access to any authenticated account | 🔴 High | A `curators` table plus `is_curator()` SECURITY DEFINER with a fixed `search_path`. Disabling signup in the dashboard is still pending — see §7 |
| ✅ BL-04 | `apple_id` without `UNIQUE` broke the import's upsert | 🟡 Medium | `UNIQUE (apple_id)`, verified by gate G12 |
| ✅ BL-05 | Non-idempotent seed: running it twice duplicated the 38 questions | 🟡 Medium | `UNIQUE (prompt)` on `questions`; every seed block uses `ON CONFLICT DO NOTHING` on a natural key |
| ✅ BL-06 | The `field_report_aggregates` view without `security_invoker` bypassed RLS | 🟡 Medium | `WITH (security_invoker = on)`, verified by gate G8 |
| ✅ BL-07 | `place_tags` with `SELECT using (true)` leaked ids of unpublished places and admin-only tags | 🟢 Low | Policy with a double `EXISTS` (published place **and** active non-admin tag). Anon sees 0 of 145 today |

---

## 2. Divergences in the source material

Found by cross-reading the PRD × the product's `CLAUDE.md` × `PLAN.md` × `schema.sql` × the CSV. None blocks the build; recorded so they are not rediscovered.

| # | Divergence | Resolution |
|---|---|---|
| ✅ BL-08 | `Hype trap` is cited in PRD §6.4 but does not exist among the seed's 93 tags | Created in `character` with `admin_only = true` (S04). Anon sees 93 tags, not 94 |
| BL-09 | PRD §9.10 describes a `Collection` primitive unifying codes, curated lists and a shortlist — it does not exist in the schema | The shortlist left the MVP; `codes.highlighted_places` covers the remaining case. Reopen only if the shortlist comes back |
| BL-10 | Trip Builder is in PRD §5 as in-scope for v1, but appears in none of the 9 phases of `PLAN.md` | Out of the MVP (§4 below) |
| ✅ BL-11 | The PRD says "single-user authentication"; curation is done by two people | **The PRD was right.** Decided in S04: one account only (Michael's), which Edu also uses. The `curators` allowlist remains the mechanism — just with one row |
| ✅ BL-12 | The CSV's `Breakfast & brunch` tag does not exist in the vocabulary of 93 | Mapped to `cuisine/breakfast-diner` in the import (S04). 54 places, `source = 'suggested'` |
| ✅ BL-13 | `Dallas–Fort Worth` in the CSV uses an en-dash, not a hyphen | Normalized to a hyphen in the import (S04). Gate G10b guarantees no city value carries an en-dash |

---

## 3. Accepted technical debt

| # | Item | Reason |
|---|---|---|
| ✅ BL-14 | The 5 skills still carried WiseFacilities examples — objects that do not exist here | Rewritten in S04 against the real objects. `naming` also had a worse risk than orphan examples: it prescribed **Brazilian formatting** (`1.234,56`, `DD/MM/YYYY`, `R$`), in direct contradiction with ADR-02. `spec-format` was marked as unused (ADR-04), preserved only as a reference |
| BL-15 | `docs/GANTT-MichaelinMap.csv` and `docs/DOMAIN_QUESTIONS.md` are filled with the template's example content and are not maintained in this project | ADR-04. Files preserved but out of use — do not read at boot, do not update. **S09 note:** for the same reason they were left out of the translation to English; translating a file nobody reads is cost without return. Deleting them is a defensible cleanup whenever someone wants it |
| BL-16 | `docs/files/CLAUDE.md` (from Claude Web) coexists with `.claude/CLAUDE.md` and diverges from it | `.claude/CLAUDE.md` is canonical. The one in `docs/files/` is source material |
| BL-36 | **The boot prompt filenames are still in Portuguese** — `docs/prompts/01-orquestrador-cli.md` and `02-executor-cli.md` — while everything else in the repository moved to English in S09 | **Deliberate, not an oversight.** The `/orquestrador` and `/executor` slash commands live outside this repository, in the user's global Claude Code config, and point at those exact paths. Renaming the files breaks both commands **silently** — the slash command still exists and simply fails to find its prompt. Renaming is possible, but it is a two-place change: the files here and the skill definitions outside. Recorded so that whoever notices the inconsistency and decides to "finish the translation" knows what it costs. The file *contents* are fully in English, and the only Portuguese left inside them is intentional: the sentence the CLI declares to Edu at boot, since the conversation stays in PT-BR |
| BL-17 | No audit table — only `places.updated_by` + `updated_at` | With **one** curator account (S04), `updated_by` is constant and attributes an edit to nobody. `updated_at` remains useful (it feeds F-02's stale list). Reopen only if a second account appears |
| BL-21 | `npm audit` reports 2 high vulnerabilities in `react-router` (GHSA-qwww-vcr4-c8h2, CSRF bypass in **RSC mode**) | **Does not apply**: a SPA with no React Server Components. The "fix" would be a major downgrade to 7.11.0. Revisit when the advisory is updated |
| BL-22 | Vitest not configured | **The prediction came true in S06** and again in S07: 27 filter checks, then 60 for the codes engine and the Roulette, all through throwaway harnesses and none versioned. **S07's technique is worth recording:** `npx vite build --ssr harness/x.ts --outDir harness/dist` bundles a TS file resolving the `@/` aliases, and `node harness/dist/x.js` runs the result — so the harness exercises the real modules, not a copy. With a ten-line fake `document` you can even test the code that writes CSS variables. It is the shortest path between "no Vitest" and "actually tested", and also the ready-made outline of the suite when it comes. **Third repetition in S08** (61 checks of the field-reports engine), and now with the strongest argument in favor of versioning: F-06 had all 61 checks passing and **still had two interface defects** — a receipt that never appeared and a counter that double-counted — which only showed up when the page was opened. A suite would not have caught those two; what it catches is the regression of the 61 on the next change, which today nobody catches. **Fourth repetition in S10** — 17 checks over the new admin tag filter, again throwaway, again outside the repo. That one mattered more than the count suggests: the filter's predicate feeds a bulk mutation, so a wrong predicate would confirm the wrong rows. The checks that earned their place were the `facet:slug` collision, the fail-closed on an unknown key, and the equality between what the bulk bar counts and what the statement writes |
| BL-30 | **Six of the seven tag facets are dormant.** `format`, `occasion`, `vibe`, `logistics`, `dietary` and `character` have zero assignments among published places, so the F-04 panel does not render them | Not a bug: by design a facet with no populated option does not appear, and they surface on their own as Michael tags, with no deploy. Recorded because it is the visible symptom that curation (bible §13.1) is the critical path — the filter is ready and waiting for data. **Updated twice in S08.** With the rating facet removed (RN-30) and `suggested` tags hidden from the public (RN-31), the panel today offers **star, type and area — three facets, none of them tags**. The 5 cuisines that used to appear were all import guesswork and went with them. **The measurement that settles it:** of the 145 assignments, **zero** are the curator's; of the 58 published, 11 have some tag and none has `the_dish`; 21 of the 94 tags have ever been used. And Edu **cannot tag on Michael's behalf** — he has never been to any of these places. In other words: this is not debt waiting for a CLI session, it is work that depends on one specific person. See `BL-34` for what can be unblocked without her. **S10 changes the framing, not the fact.** The reason the queue never moved was partly that it was unworkable: the visitor had full faceted filtering by cuisine while the curator — the one person who has to touch tags — had no tag filter at all. That asymmetry is fixed (`?tag=cuisine:tacos&tagSource=suggested` is now a link), so the 173 pending suggestions are ~20 coherent batches rather than one flat list. Whether Michael works them is still the open question. **S11 breaks the zero.** The measurement above no longer holds: of 226 assignments, **5 are the curator's** — `Breakfast & Diner` confirmed across 24 Diner, Colleen's Kitchen, Geraldine's, June's All Day and Laurel Restaurant, by Edu, in the bulk bar. `cuisine` is the first facet to render publicly. It also took a bug fix to get there: the first attempt reported success and wrote nothing, because RLS does not raise — it disappears rows (`d9e49cd`) |
| BL-23 | No light/dark theme choosable by the visitor | **Partially resolved in F-05 (S07).** A code now repaints `--primary`, `--background` and `--foreground` at runtime, and a dark background turns on the `dark` class, which is what makes borders, muted text and accents follow along instead of leaving light seams. What does **not** exist is a selector: the code owns the theme, and that is why `code-effects.ts` can be the exclusive owner of the `dark` class. Creating a theme selector later requires deciding who wins when both speak. `next-themes` is still installed as an orphan dependency — remove it if nothing starts using it |
| BL-25 | Bundle above Vite's 500 kB warning | **Partially resolved in S05.** MapLibre landed and took the single bundle to 1,647 kB (450 kB gzip), which was too much weight even for someone who only opens the city gate. The map became `lazy` + `Suspense`: main bundle 699 kB (204 kB gzip), map chunk loaded only when a city opens. **Chunk figure corrected in S06:** it is 1,030 kB (274 kB gzip), not the 947 kB recorded here — that value dated from MapLibre 6; v5 embeds the worker in the same file. The main bundle still exceeds 500 kB — the next candidate is splitting by route (admin × public), since a visitor never loads the admin |
| BL-26 | The 4 `logistics` tags marked `is_derived = true` (`Open late`, `Open Monday`, `Closes early`, `Open for breakfast`) were meant to be computed from opening hours, which left with ADR-06 | There is no automation behind them. They stay in the vocabulary as curator-assignable. Decide in F-02 whether the admin should still treat them differently, or whether `is_derived` becomes a dead field |
| BL-27 | The migration `20260806120100_f01_seed_and_import.sql` (155 kB) was applied through `execute_sql` in blocks, not through `apply_migration` — a payload of 511 rows was too large for a single call | The file in `supabase/migrations/` is the real artifact and `schema_migrations` was sanitized by hand to reflect that. Database contents verified against the CSV by checksum. If another bulk import comes up, use the dashboard SQL Editor |
| ✅ BL-35 | **No migration published a place.** The 58 `published` came from an ad-hoc `UPDATE` in S05 and existed only in the live database, so applying the migrations to a fresh project returned 511 places with none published and a public guide that rendered **empty while looking healthy** — the city gate counts published places, so it showed no city at all, with no error and no clue | **Closed in S10** by `20260808120000_publish_launch_batch.sql`. The criterion from bible §12 (`starred OR tier = 'destination'`) was verified against the live data first and reproduces exactly the 58, with zero difference in either direction. Idempotent by end state: it updated zero rows against the live database and publishes 58 against a rebuild, and 6 gates assert the end state either way. `status = 'unreviewed'` sits in the predicate so a place the curator later moved to `closed` or `hidden` is never silently republished; `updated_by` is deliberately left null because it is an FK to `auth.users`, which does not exist yet on a fresh project. Rollback written, with the warning that running it takes the public guide from 58 places to zero |
| 🔽 BL-29 | **The map draws no geometry in the Chrome the CLI drives.** Markers appear and position correctly; no tile renders | **Downgraded in S08: not a product defect.** Edu reported that he **sees the map normally in Firefox and on his phone** — so the guide works for whoever uses it, and what remains is this machine's automated browser. This **contradicts the S05 record**, which claimed reproduction in Chrome, Firefox and Edge with a plain MapLibre from a CDN; either the environment changed between August and now, or that test did not isolate what it was thought to. Neither was redone — this is Edu's report, not my verification, and it is written as such. **Practical consequence:** stop treating it as a bug and start treating it as a limitation of visual inspection by the CLI. When a session needs to genuinely check the map, Edu is the one who looks. Reopen only if someone reports a mute map in a real browser. **S10 note, recorded without any claim attached:** during the visual verification of the redesign the Austin map drew complete tiles in the Chrome the CLI drives — roads, water, labels. That is consistent with the intermittency already documented below (absent in S07, back in S08) and is **not** evidence that anything changed; no map code was touched beyond the container, the border and the pin palette. Written down only so a future session does not read it as a contradiction. **Previous history, kept:** intermittent — it stopped in S07, came back in S08. In the S08 verification (removal of the rating facet) the Austin map drew the pins positioned correctly and **zero geometry**, a flat background, on both screens. Between S07 and S08 our map code did not change. **What the intermittency adds:** the environmental hypothesis gets stronger, because a cause in the code does not come and go on its own between two sessions with no deploy. It still is not worth redoing the investigation — the record below remains valid and the next step remains Edu's. **The S07 record, kept:** it stopped reproducing — symptom absent, cause not explained. In F-05's visual verification the map drew complete geometry: highways with labels (`Express 183 Toll`, `Loop 1`), parks and water, in both the code's dark style and the default. Nothing in the map code changed beyond the runtime style swap F-05 added. **I do not know what changed in the environment** and I did not go looking — the investigation was already exhausted and the recorded next step was Edu's. It stays open with a warning: if it happens again, the record below still holds and should not be redone. An adjacent detail from the same session may or may not be from the same family: Chrome refused `localhost:5173` and `127.0.0.1:5173` with the port listening, and answered only on the network IP (noted in `.claude/CLAUDE.md`). **The original investigation's history:** **it is not our code — proven.** A plain MapLibre map, loaded from a CDN onto a blank page, without a line of ours, behaves identically: `sourcedata:openmaptiles:pending` forever, never `loaded`, zero rendered features, **zero errors**. Reproduces in Chrome, Firefox and Edge. **Ruled out with evidence:** WebGL (2.0, AMD GPU through ANGLE), Web Workers (a blob worker answers normally), page-level network (a `fetch` of the TileJSON brings 19 kB and of a real tile brings 116 kB, both 200, from the page's own context), style, sprite, fonts, coordinates, canvas size, CSS, and both MapLibre versions (6.2 and 5.24). **What remains:** something on this machine interferes with network requests made from inside MapLibre's *worker* — antivirus, endpoint filtering or a system proxy are the suspects, since the same request works from the page. Next step: test on another machine or network. Nothing to fix in the repository until then |
| BL-31 | **Two admin screens have never been seen running: `/admin/codes` (F-05) and `/admin/reports` (F-06).** Both compile and pass the linter, but they are behind the curator login and the CLI does not have Michael's password | The public side of both features was fully checked in the browser; this is what is left. **From F-06, the runtime risk that could be eliminated without logging in was eliminated:** the PostgREST embed of the queue (`places(name,slug)`, `questions(prompt,input_type,unit_label)`) was fired against the database separately and resolves — a wrong relationship would have blown up there. Edu opens and clicks: in Codes, create a code, save it and redeem it in the guide; in Reports, approve/reject a free-text answer and seed a place. **For the queue to have anything to show, a `pending` answer has to exist** — that is, somebody has to answer one of the 4 free-text questions in the guide. They are 4 of 38 in the draw, so it may take opening several different places before one comes up. **S09 update:** the login was tested and works; Edu reset the password through the Supabase dashboard, since the password lives in `auth.users.encrypted_password` and is readable by nobody. **S10 update: this is now three surfaces, not two.** The whole admin was restyled (7 screens) and the places list gained a tag filter with a bulk-confirm bar — all of it build-clean, lint-clean and colour-audited, none of it seen running. The pattern from S08 is the warning: 61 assertions passed and the eye still found two interface defects that assertions cannot reach. One logged-in session closes all three |
| ✅ BL-32 | `src/pages/admin/placeholder.tsx` was left unused when F-05 replaced the `/admin/codes` route | Deleted in S08 with Edu's authorization, after confirming by sweep that nobody imported `AdminPlaceholder`. Clean build afterwards |
| BL-33 | The `DEMO` code seeded in F-01 carries `theme.mapStyle = "amber"`, which was never a map style | Harmless — `mapStyleUrl()` falls back to the default instead of handing MapLibre an invalid URL, and there is a check covering exactly that. Fix it through the admin the first time Edu opens the screen, or delete `DEMO` once a real code exists |
| BL-37 | **A Code cannot repaint part of the interface it should own.** `MANAGED_PROPERTIES` in `code-effects.ts` covers `--primary`, `--foreground`, `--background`, `--card`, `--popover` and `--ring` — but **not** `--brand-ink` or `--secondary`. S10 routed load-bearing UI through both: the selected facet label, the "Picked for you" badge and the admin nav read `--brand-ink`; every form control (input, textarea, select trigger) moved from `bg-transparent` to `bg-secondary`. Under an active code those surfaces stay frozen at the paper values. The input change is a straight regression — `bg-transparent` used to inherit the code's surface for free | ⬜ Open, and it is **one decision, not two**: it is the same question as the `mapStyle` gap below — *what does a Code have the right to repaint?* A code that sets a dark `theme.background` but no `theme.mapStyle` also gets a dark guide with a light map panel, because MapLibre paints its own style background. Both are one-liners once the principle is settled; both touch `code-effects.ts`, which has F-05's 60-check harness around it, so the harness re-runs with the change. Found by the S10 adversarial audit (H-02) and not fixed there on purpose |
| ✅ BL-38 | **The 53 tag suggestions from the S10 Opus 5 batch are written but not applied.** A research pipeline over the 17 published food places with no cuisine returned 53 assignments across 16 places with a source URL and a quote per tag, in `cuisine`, `format`, `logistics` and `dietary` | ⬜ Blocked on `DP-10` and `DP-11`. Applying them takes published food places without a cuisine from **17 down to 4**, and would be the first assignment ever in `format` (1), `logistics` (0) and `dietary` (0) — the facets `BL-30` calls dormant. Everything enters `source = 'suggested'`, invisible to visitors under RN-31 until Michael confirms. The rollback is surgical, in the shape of `20260807140000`. **Applied in S10 as `20260809120000_suggest_researched_tags.sql`** — 53 assignments across 16 places, published food places without a cuisine down from 17 to 4, the four remaining being `DP-10` and `DP-11`. Gate G1 rejected the first apply over a miscount of mine (52 expected, 53 present); the transaction rolled back whole and the database stayed intact |
| BL-39 | **Nine minor findings from the S10 visual audit, accepted rather than fixed** | ⬜ Open, none user-blocking. Primary button hover is 1.09:1 (below perceptual threshold); the active tab reads recessed on the ink surface because `--card` is darker than `--muted` there; `DialogFooter` was re-padded and is imported by nothing; the empty-state rule (icon + title + explanation + next step) was applied to the guide and skipped in `codes`, `places` and `city-gate`; the map↔list selection rail is 1.50:1 and renders as a short arc on a rounded container; two half-pixel type sizes were fixed but `--radius`-derived values remain off the 4px grid. Recorded so the next visual pass does not rediscover them |
| BL-40 | **Inter was never approved as a dependency.** The design system specifies it; the project ships Geist (`@fontsource-variable/geist`) and stayed there through the whole S10 redesign | ⬜ Open and inert. Asked twice in S10, unanswered, so the assumption was stated and the work continued on Geist — the design-system skill records this explicitly so no future agent "fixes" components to Inter. Swapping is one package plus one line in `index.css`. Nothing depends on it |
| ✅ BL-41 | **The cuisine research pass over the 117 unreviewed Austin restaurants.** Scope, prompt and results in `docs/research/` | ✅ **Complete in S12 — 117 of 117.** 91 taggable, 15 with no honest slug, 10 closed, 1 uncertain. Evidence 103 Tier A / 37 Tier B / 13 Tier C; 28 of the 38 slugs used. Full record and method notes in `docs/research/README.md`. The trap held: `cuisine-117-batch.csv` was never regenerated, so every `id` still points at the restaurant it was cut for. **Nothing has been applied to the database** — see `BL-43` (the tags), `BL-44` (the closures) and `BL-45`/`DP-11` (the vocabulary). The 89 Austin bars remain deliberately out of scope; S12 supplies a reason to revisit that, since `cocktails-bar-food` turned out to have four correct instances and a real meaning — see `BL-42` |
| ✅ BL-43 | **The 91 cuisine suggestions from the completed research pass are written but not applied.** `docs/research/cuisine-117-results.json` holds one object per place with a source URL and a verbatim quote per slug | ⬜ Open, and it is one migration in the shape of `20260809120000_suggest_researched_tags.sql`: insert `place_tags` with `source = 'suggested'`, resolving places and tags by natural key. Everything enters invisible to visitors under RN-31. **✅ APPLIED IN S14 as `20260810130000_suggest_researched_cuisine_117.sql` — 122 assignments across 106 places**, after `20260810120000` added the seven slugs. `place_tags` went 226 → 348, and **the increase is exactly 122**, which is the proof nothing was overwritten: the primary key is `(place_id, tag_id)` and `ON CONFLICT DO NOTHING` skips collisions, so a full-count increase means every pair was new. Rollback written and it **cannot** delete a confirmed tag — `source = 'suggested'` sits in its WHERE, and it reports how many rows it spared. **The join key is `(name, whitespace-normalised address)`, and measuring that before writing is what saved the migration:** name alone left 9 rows ambiguous across five two-location brands, and the *exact* address matched only 42 of 117 because the database kept a double space before the ZIP that S11's checksum had normalised away in the CSV — an exact join would have inserted 42 rows, raised no error and looked like success. Historical note on what was blocking — S12 had no MCP, so writing SQL was off the table by the project's first rule. **✅ S13 UNBLOCKED THIS.** All four blocking rows were re-run and resolved, and the vocabulary extension that 17 of the assignments depend on is written (`20260810120000`). The count is now **106 places, not 91** — every open place in the batch carries at least one cuisine tag. **Order matters: the vocabulary migration must be applied first**, or 17 assignments reference tags that do not exist. Historical note on what was blocking: `006 Anthem` and `009 Bar Toti` must be re-run (both used `cocktails-bar-food` as a crossed-kitchen fallback; useful new context is that Bar Toti and `034 Este` are sister restaurants sharing 2113 Manor Rd); `008 Aris` needs its second evidence row dropped, since it cites an OpenTable page for *Iris*; `012 Bellissima` is the weakest row of the 117 and deserves one look from a normal browser, because `bellissimatx.com` returns 403 to every fetch. Also flagged: `090 Santa Catarina` was tagged `tex-mex` **against** its own site's claim of "interior Mexico" — deliberate, and recorded so Michael can flip it |
| BL-44 | **Ten places in the batch are closed and one cannot be established — and none of that is a tag.** Found by the S12 research pass while looking for cuisines | ⬜ **Michael's**, not a migration's: `status = 'closed'` is a statement about a real business, and ten at once changes what the guide claims. The list, with dates and sources, is in `docs/research/README.md`. **Four closed during 2026 and three of those were Michelin-distinguished** — `077 Olamaie` closed 19 Jul 2026 *holding a star*, `080 Otoko` after ten years of omakase, `081 PastaBar` in February. `021 Chapulín Cantina` and `113 Vespaio` are two records of one event, sister restaurants on S Congress. `060 Ma'coco` is the unresolved one: Yelp says CLOSED, the brand's own site still lists it — the `DP-10` shape, settled by a phone call and not by more searching. **This is ~9% of an unreviewed backlog assembled from map albums over years, and it is the evidence `DP-10` was missing:** the same sweep over the **58 published** places is worth more than any tagging, because those are the ones a visitor can reach |
| ✅ BL-45 | **The cuisine vocabulary has no plain `american`, and `new-american` is absorbing the gap.** Fifteen of the 117 researched places had no honest slug; five of them are ordinary American restaurants | ⬜ Open, **Michael's** under RN-13, and it is the same fact seen from both ends. Places with nothing to take: `035 Finley's` (a 1940s bungalow bar — wings, deviled eggs, tots), `057 Lou's` (rotisserie, sandwiches, breakfast tacos, frozen custard), `050 Knotty Deck`, and `018 Captain Pete's` + `098 Sundancer Grill`, two Lake Travis restaurants that produced the same suggested label. The only American entries are `new-american`, a chef-driven category none of them is, and `southern-comfort`, which their menus are not. **From the other end: `new-american` was used 18 times in 117, more than any other slug and 60% more than the next** — a slug meant for one style doing duty for a whole cuisine. `067 Muck & Fuss` is the control: also a bar with a kitchen, and `burgers` lands cleanly because its food has an identity. **The gap is not "bars" — it is "ordinary American."** Same family as `DP-11` and `BL-42`, on a third slug. **✅ ANSWERED IN S13.** Edu approved `american` as one of seven additions; it alone unlocked four places (`035` Finley's, `057` Lou's, `066` Millie's On Main, `098` Sundancer Grill) and it is the structural fix for the `new-american` inflation, which had reached 18 of 117. Written as `20260810120000_extend_cuisine_vocabulary.sql`, **not yet applied** — no MCP in S13 |
| ✅ BL-46 | **`cajun-creole` is a single missing cuisine, and it is exactly the case `german` already answered** | ⬜ Open, **Michael's** under RN-13. `114 Vic & Al's` is a self-described family-style Cajun restaurant — gumbo, crawfish étouffée, po'boys, seafood towers — with nothing in the 38 slugs to take. **`southern-comfort` was refused on principle:** the vocabulary already separates `tex-mex` from `interior-mexican`, so it plainly cares about regional precision, and folding Louisiana into the American South throws away exactly the distinction it makes elsewhere. The gap surfaced twice in the pass — `093 Shore Raw Bar` also draws on Louisiana and was saved only because `seafood` carries its identity. **`german` was added on three instances and resolved all three** (`004`, `038`, `051`), which is the strongest argument on record that answering these is cheap and pays. **✅ ANSWERED IN S13** together with `american`, `mexican`, `georgian`, `sandwiches`, `wine-bar` and `brewery` — seven additions chosen as the smallest set that gives every open place in the batch a cuisine tag. Same migration, same caveat: written, not applied |
| ✅ BL-48 | **The facets pass over the 58 published places is paused at 10 of 58, on evidence.** It targeted `format`, `logistics` and `dietary` — the three facets with 1, 0 and 0 assignments in the whole database | ⬜ Open, and the pause is the finding: `format` filled 9 of 10 but **almost always with `sit-down-restaurant`**, while **`logistics` and `dietary` filled 1 of 10 each**. Those two are not researchable from the open web at scale — restaurants do not publish "cash only" or "parking is a problem", which is exactly why the facets are empty. **What the ten did produce justifies restarting it with a different target:** `009 Chez L'Amour` is **closed and published in the guide right now** (St. Augustine's own tourism site titles the page "Permanently Closed"), which is one in ten — the same rate as the unreviewed set, projecting to ~6 of the 58. **Restart it as a closure sweep** collecting status, opening hours (the raw material the four `is_derived` logistics tags have never had, since `places` holds no hours column) and the publishable half of `vibe` (patio, rooftop, dog-friendly, live music). Scope frozen and checksummed in `docs/research/facets-58-batch.csv` (`5e13fc48…`), derived offline and reconciled three ways. Directly serves `BL-44` and `DP-10`. **✅ THE CLOSURE SWEEP RAN IN S14 over all 58** and is recorded in `docs/research/closures-58.md`: 54 open, 3 closed, 1 uncertain. `DP-10` answered. The facets half stays parked at 10 of 58 for the reasons above — `logistics` and `dietary` are not researchable at scale. **A fourth false positive was avoided by address:** `049 Sway` reads closed by name, because two other locations of the brand are, and ours on Bee Caves Rd is open |
| BL-47 | **Two of our own place records carry the wrong street address.** Found incidentally while verifying restaurants against their own sites | ⬜ Open, low urgency, and harmless today because every migration joins by name. `064 Mattie's` — our record says 901 W Live Oak St; the restaurant's own site and every source say **811**. `103 The Kimberly` — ours says 200 W 7th St, the restaurant says 200 W 6th St Suite 100 with entrance and valet on W 7th; both describe one building. **S14 added a third and it is worse than the other two:** `055 Vaudeville` carries `city = 'Austin'` with an address in **Fredericksburg**, 80 miles away. The `city` column drives the public city gate, so this record shows up under Austin for a visitor. Worth fixing before anything ever joins on address. **A related hazard that is not a defect:** `060 Ma'coco` and `061 Ma'CoCo` are one brand spelled with a straight apostrophe in one row and a curly one plus different casing in the other — a `grep` for either does not find the other, which is how the pair was nearly missed |
| BL-42 | **`cocktails-bar-food` is a claim about format sitting inside the cuisine facet**, and it attracts every place the vocabulary cannot describe | ⬜ Open, low urgency, recorded before it becomes a habit. The S11 research pass reached for it twice as a fallback for crossed kitchens — Anthem and Bar Toti, the latter a self-described bistro — which is the same failure `interior-mexican` is already showing in `DP-11`: a slug that means one thing quietly becoming the bin for "none of the above". A visitor filtering Cuisine by it expects a drinks-first place. The prompt now closes it off explicitly, so this is about the vocabulary, not the pipeline: either it moves to `format`, or its meaning gets written down somewhere the next researcher reads. **S12 supplies the material to close this by example rather than by argument.** Four correct uses, all drinks-first by the venue's own account: `068 Murray's Tavern` ("a newly designed cocktail tavern", menu built around two of the owner's grandmother's cocktails), `083 Péché` (Austin's first absinthe bar — the owner set out to build a cocktail bar and chose absinthe to stand apart), `094 Sidecar` (a basement cocktail club, evenings only, deliberately short menu), `105 Tiki Tatsu-Ya` (21+, the drinks are the whole proposition). Plus `109 Uchibā`, tagged `japanese` + `cocktails-bar-food`, which shows **the slug can coexist with a cuisine instead of replacing one**. Against those: the two misuses are still `006 Anthem` and `009 Bar Toti`. Write the meaning from these seven rows |
| BL-28 | `get_advisors(security)` reports 8 WARNs of "SECURITY DEFINER executable by anon/authenticated" | **Accepted, not a finding.** They are `is_curator()` (the RLS policies need to execute it), the two RPCs that are public by design (bible §11), and `rls_auto_enable()` — a Supabase platform *event trigger* that enables RLS on any new table in `public`. Since it returns `event_trigger`, it is not callable through PostgREST. None of them exposes data: our three have a fixed `search_path` and filter on output. Revisit only if a new RPC appears |

---

## 4. Out of the MVP

Cut with a reason. Do not repropose without a new fact.

| Item | Reason for the cut |
|---|---|
| Dedicated review-queue screen (F-02) | Cut in S05. The three queues — 28 tier conflicts, pending suggested tags, 15 without a type — are already cards on the Overview linking to the list with the filter applied. A screen of its own would be a fourth way of looking at the same records, at the cost of keeping two places in sync |
| Google Places API + hydration of the 511 | ADR-06 — price is the curator's judgment; hours are solved by the directions button |
| My Maps KML sync + `sync_runs` + sabotage test | ADR-08 — mobile quick-add is already a complete capture path. **Reassessed in S09 at Edu's request and not reopened:** the places come from Apple Maps, not Google My Maps, so the sync would read an empty source; and KML never carries the judgment layer, so it would bring more pins, which is what §1 says has no value. The cheap version, if it is ever built, keys on a link-shared map id — not on an account, which has no official API |
| SEO: sitemap, JSON-LD, dynamic meta, pre-render | ADR-07 — the guide is unlisted |
| Trip Builder | Large scope, and the "out-of-towner" context is secondary for a guide among friends |
| Settle It (head-to-head bracket) | The PRD itself (§4) discarded group decision-making as a primary context |
| I'm Hungry Now (open now, near me) | Depends on opening hours, which left with ADR-06 |
| Bad Idea | Low marginal value over the `character` filter + Roulette |
| Local shortlist | With no account and no server, the gain over bookmarking in the browser is small |
| Area by geographic polygon | `area` becomes an optional manual field in the admin |
| TanStack Table | A simple list is enough for 511 rows and 2 users |

---

## 5. Open decisions

| # | Item | Who decides |
|---|---|---|
| DP-06 | Does Michael want to appear in the guide — face, taste profile, an "about" page? | Michael. Affects copy and tone, does not block the build |
| DP-07 | Voice notes per place — high personality value, moderate scope | Michael. A candidate for a future phase |
| DP-08 | The 28 tier+unvisited conflicts, the 15 without a classification and the 4 outside the tier × type crossing | Michael, in the admin's review queue (F-02). The 28 are already flagged in the database: `'CONFLICT:TIER+UNVISITED' = ANY (source_guides)` |
| DP-09 | The 145 `source = 'suggested'` tags written by the import need confirming or rejecting | Michael, in F-02. The admin has to distinguish them visually from curator tags (RN-15). Reverting everything is `DELETE FROM place_tags WHERE source = 'suggested'`. **S10 made this workable**: the admin list now filters by a single tag and confirms it across every filtered place at once, so the queue is ~20 coherent batches instead of 226 unrelated decisions. **S11: the first batch was worked, and not by Michael.** Edu confirmed `Breakfast & Diner` on 5 places — 5 curator, 221 suggested. He decided in S10 that he confirms `cuisine` specifically, because verifying a researched fact is not the same act as tagging in Michael's place; the other six facets stay Michael's. Worth recording that **he did not audit the 5 before confirming** — so that batch's precedent is "the pipeline was trusted", not "the pipeline was checked" |
| ✅ DP-10 | **Is `Gina's on Congress` still open?** It is `published` and `starred` in the guide right now. The S10 research batch found three independent closure signals — Yelp lists it CLOSED, Corner marks it Permanently Closed as of April 2026, and KVUE reported in January 2026 that staff at parent company Flavor Hospitality Group had gone unpaid and the owner was unreachable. The counter-signal is that the restaurant's own site is still live with hours | **Michael** — he is in Austin, and a phone call settles it faster than any amount of research. If it closed, it is `status = 'closed'` (ADR-03), which the schema already supports. Worth knowing that 511 places came from Apple Maps albums collected over years: it is statistically unlikely this is the only one, and the 58 published are the ones a visitor can reach. **S12 turned that from a hunch into a measurement: 10 of 117 unreviewed places are closed and 1 more cannot be established** — see `BL-44`. Four of those closed during 2026 and three were Michelin-distinguished, so recency and reputation protect nothing. The sweep over the 58 published is no longer a suggestion. **✅ ANSWERED IN S14: Gina's on Congress is CLOSED** (Yelp CLOSED, ~April 2026), and it was `published` **and starred** the whole time. The sweep ran over all 58 and found **3 closed and 1 uncertain** — Gina's, `057 Vince Young Steakhouse` (24 Jan 2026, after 15 years, covered by KVUE/KXAN/CBS Austin) and `009 Chez L'Amour`, plus `020 Fabrik` reported temporarily closed. ~5% against 9% in the unreviewed set. **The restaurant's own live site outlived the restaurant by months**, which is the lesson worth keeping: a live site is a weak counter-signal. Full record in `docs/research/closures-58.md`. Setting `status = 'closed'` on the three is `BL-44` and remains the curator's |
| DP-11 | **The cuisine vocabulary does not cover the top of the guide.** Seven of the 17 places researched in S10 either got no cuisine or got one the researcher itself flagged as wrong: Ranch 616 ("South Texas ice house" — its own menu says it is neither Tex-Mex nor Southwestern), Cosmo (pan-global buffet), Space Cowboy (deliberately cross-cultural), Ember Kitchen (contemporary Latin), Nomade (Yucatecan, which is coastal, filed under `interior-mexican`), Roya (Persian, filed under `middle-eastern`), Verbena (contemporary Mexican with French technique) | **Michael.** The 37 cuisine slugs were designed for a taco/BBQ/Tex-Mex Austin; the 58 published are the top of the guide, where places are idiosyncratic. The concrete hole is a missing `latin-american`. Worse than the gaps: `interior-mexican` is becoming a dump for "Mexican that is not Tex-Mex", which is false classification wearing the face of precision. RN-13 keeps the vocabulary controlled, so adding a slug is his call, not the CLI's. **S11 answered one half of it: `german` exists** (`20260809130000`, 38 slugs, sorted between `french` and `spanish`). The research pass over the 117 unreviewed restaurants hit the wall three times in its first ten — Alpine Haus and Krause's Cafe in New Braunfels, Friedhelm's Bavarian Inn in Fredericksburg — and that is structural, not coincidence: those are German settlements and the Hill Country still cooks that way. Edu decided it, and the split is worth keeping: **`german` is a factual category with three confirmed instances; `latin-american` is a boundary judgment and stays Michael's.** Still open, and getting worse with volume: `interior-mexican` as a dump for "Mexican that is not Tex-Mex". **S12 documented that from four directions across 117 places, and the evidence is now unusually strong.** (1) The slug would have been *actively wrong* three times and was saved each time only because another fit: `034 Este` is coastal Mexican, `111 Veracruz Fonda` is from a Gulf state, `061 Ma'CoCo` is Baja. (2) Three records had no honest Mexican slug at all — `060`, `061` and `112 VERDAD` — because the vocabulary offers only `tex-mex`, `tacos` and `interior-mexican`, with nothing for Baja/California-style, nothing for modern pan-Mexican, and **no plain `mexican`**. (3) The label is being claimed by places it does not describe: `090 Santa Catarina` opens its own site with "the vibrant flavors of interior Mexico" and serves fajitas, house tortillas and margaritas — the mechanism seen from the inside. (4) **The slug ends the pass with one defensible instance in 117 places** — `063 Manuels`, which claims "Regional Mexican Cuisine" itself — after `033 El Naranjo`, the cleanest use of all, turned out to have closed |

---

## 6. UX and polish

| # | Item | When |
|---|---|---|
| ✅ BL-18 | Authored copy for an impossible filter combination — the exact moment the visitor would leave, and the best place in the product to be funny | Done in F-04 (S06). "Nothing is all of those things. Michael has opinions, not miracles…" — the joke also teaches the AND semantics (RN-16), which is the one thing a faceted panel never makes obvious on its own |
| BL-19 | Accessibility: filters navigable by keyboard, sufficient contrast in every Code theme, map information available in the list for screen readers | **Contrast closed in F-05 (S07)** and closed at the source: `contrastOn()` derives `--primary-foreground` and `--foreground` from the WCAG luminance of the color the curator picked, so no code theme can be born illegible — there is nothing to police component by component. Verified (`#F5C518` → nearly black text; `#101014` → nearly white text). **Still open:** keyboard navigation through the filters, and the map's information being available in the list for screen readers |
| BL-34 | **Content is bottlenecked on one person, and it is worth separating what can be unblocked without her.** Established in S08: Edu cannot tag and cannot write `the_dish` — he has never been to any of the 511 places | **The machine's part: done in S08.** The migration `20260807140000_suggest_cuisine_published` suggested 28 cuisines, taking published food places without a cuisine from 45 down to **17**. All `suggested`, invisible to the visitor under RN-31, waiting for bulk approval in the admin. The rollback is written and surgical: it deletes only these 28, touching neither the 145 from the import nor anything Michael has already confirmed (confirming turns them into `curator` and the rollback skips them). The 17 that remain are named in the migration's footer — they are the ones that neither the name nor public knowledge resolves. Only `cuisine` and part of `format` are inferable from name and type; `vibe`, `logistics`, `dietary`, `occasion` and **all** of `character` require having been there and **must not be generated** — they would be false, and false in Michael's voice. **What only he can do, and it is smaller than it looks:** `the_dish` and `curator_note` on 8-10 of the strongest places. Nothing needs to be opened — they are two questions from memory per place ("what do I order here?", "why does this one matter?"). Idea recorded and not executed: prepare that short list in a format Edu can send by message and paste the answer back |
| BL-20 | Seed his own field-report answers so nothing is born at zero | **Tool delivered in F-06 (S08), seeding pending on Michael.** `/admin/reports` has the tab where the curator picks a place, answers the questions in the same UI the visitor sees, and files everything at once; the answers enter published and stay identifiable by `session_hash = 'curator'` (undoing is `delete from field_reports where session_hash = 'curator'`). **The values were deliberately not generated by the CLI** — they are observations, and an invented ceiling height would be indistinguishable from a measured one on a panel that reports to one decimal place. Two things to know before using it: seeding **reveals no aggregate** (n=5 is per place × question and one person gives one answer; the gain is that the counter is not born at zero), and there is no reason to seed all 38 — the draw shows 2-3 per visit |

---

## 7. Operational pending items in the Supabase dashboard

Not code. They need someone logged into a dashboard. None of them blocks code — the MVP closed in S08.

| # | Item | State |
|---|---|---|
| OP-01 | Disable signup in the Supabase project | ⬜ Pending, but **not a security hole**: S04's negative test showed that an authenticated account outside the allowlist is treated as a visitor — 0 places, 0 codes, writing blocked. It is hygiene, so orphan accounts do not accumulate |
| ✅ OP-02 | Create Michael's account and insert the row in `curators` | Done in S04. `mikemyday@mikecofone.com`, confirmed, seeded by `20260806130000_f01_seed_curator.sql` with `name = 'Michael'`. Verified end to end with simulated JWTs |
| OP-04 | **Deploy on Vercel.** Bible §3 anticipated "to be configured after F-03" and it fell behind | 🟡 **Repository ready as of S08; what is missing is the step that requires an account.** Already committed: `vercel.json` with the SPA rewrite — **without it every deep link (`/city/austin`, `/place/canje`) returns 404**, which is exactly what a shared link is; `public/robots.txt` reinforcing ADR-07 for crawlers that do not execute JS; and `engines.node >= 22` in `package.json`, or Vercel may pick a Node too old for Vite 8. Missing: import the repo in the Vercel dashboard and paste the two `VITE_` variables (values in `.env.local`). Step by step in the S08 log. **S09 update:** Edu decided the deploy will use the **company's** Vercel account, not a personal one, and postponed it — see `OP-05`, since the database is expected to move to the company's organization as well |
| OP-06 | **The Supabase MCP server did not load in S09, S12 and S13 — and the project side is verified clean.** Without it the CLI cannot introspect the schema, so by the project's first rule (*live schema first*) it cannot write or apply SQL at all | ⬜ **Open, and it is now the single blocker on the critical path:** two migrations are written and waiting (`20260810120000_extend_cuisine_vocabulary.sql` and the 106 assignments of `BL-43`). **S13 investigated it end to end so nobody repeats the work. Everything on the project side checks out:** `.mcp.json` is valid JSON with no BOM, `stdio`/`npx`, correct project-ref; the token has the right `sbp_` shape and **works**; approval is in place (`.claude/settings.local.json` carries `enableAllProjectMcpServers: true` and `enabledMcpjsonServers: ["supabase"]`); the package resolves and runs; and launching the server with the **exact** configured command and token completes the MCP handshake and returns **all 20 tools**, including `apply_migration` and `execute_sql`. **A plausible hypothesis was tested and disproved:** it is not a boot timeout — the server answers `initialize` in **2.0s**, twice measured, and the npx cache already holds `0.9.0`, which is the latest published version, so no download is triggered. There is also no error trail: `mcp-needs-auth-cache.json` has no supabase entry and `daemon.log` never mentions it. **The one inconsistency found**, offered as a lead and not as a diagnosis: `~/.claude.json`'s entry for this project has `enabledMcpjsonServers: []` and `mcpServers: {}` while `.claude/settings.local.json` enables it. In theory `enableAllProjectMcpServers: true` makes that irrelevant. **The missing information, and the next step: run `/mcp` in a fresh session in this folder.** It reports each server as connected, failed or never attempted — which is exactly what this investigation could not obtain from outside. If it says failed, it says why; if supabase is absent entirely, the `.mcp.json` is not being read at boot and re-approving through `/mcp` is the fix. **S13 UPDATE — the server is reachable and usable from outside the client, which narrows the fault to Claude Code's MCP client.** A ~40-line stdio client (spawn the exact configured command, `initialize` → `notifications/initialized` → `tools/call`) talks to it fine, and it was used to run the introspection this session could not otherwise do. **That produced a concrete result: the vocabulary migration written blind in S13 is now verified against the live schema** — 38 cuisine tags, `sort_order` 0–37 with 38 distinct values, none of the seven new slugs present, none carrying an assignment, and **the live ordering is identical slug-for-slug to the one the rollback declares**, so the rollback is verified too. Gate G2 will pass. **Applying it was blocked by the permission classifier**, correctly: a write to the only copy of the production database through a hand-rolled client is exactly what should need an explicit grant. So the migration is verified and still unapplied. **The practical consequence for the next session that boots without MCP:** read-only introspection is available through that path, so *live schema first* can be honoured even then — but applying still needs either a working in-session MCP, an explicit Bash permission, or the dashboard SQL Editor. **Two things NOT to do:** editing `.mcp.json`, which is proven correct, and pinning the version, which would change nothing while cache and registry agree. **Related and already recorded in `CLAUDE.md`:** killing `node` processes by time window takes this server down for the rest of a session, and only restarting Claude Code brings it back (S07) |
| OP-05 (parcial) | **Database backup outside the repository.** Today the 4 migrations reproduce nearly the whole database, because every write so far came from one of them — and that is exactly what stops holding the minute curation begins: `the_dish`, `curator_note`, `story` and any tag Michael confirms will exist **only** in the database and in no migration | ⬜ **Pending, and it is the only pending item that can cost irreplaceable data.** A clone of the repository with the database lost gives back a working app empty of judgment, which by bible §1 is the same as having nothing. **Two things to do:** (1) take a dump now (`pg_dump` through the connection string, or the dashboard backup) and keep it **outside the repo** — the dump carries Michael's entire list and his email, so it does not go into git or into a publicly synced folder; (2) **confirm in the dashboard whether this project has automatic backups** — I did not verify, and the free plan historically does not. Assuming it does is the kind of assumption you only discover is wrong on the day it matters. Raised in S09, while assessing what a handover would require beyond the repo. **S10 narrowed it, and did not close it.** A field-by-field audit found the live database reproducible from the migrations in every respect except which 58 places were published — website, price_band, last_visited, the_dish, curator_note, story, curator tags, altered tiers/tags/questions, codes beyond the seeded `DEMO`, field reports: all zero. `BL-35` versioned that one gap, so **today a clone plus the 5 migrations rebuilds everything the product has.** What remains is the future: the minute Michael writes a `the_dish` or confirms a tag — which the S10 bulk-confirm button exists to make easy — there is judgment no migration reproduces, and only a dump protects it. **Neither remaining action can be done by the CLI:** the Supabase MCP has no dump tool, `.env.local` carries only the URL and the anon key with no connection string, and credentials in plaintext are not something the CLI handles or asks for. Both are Edu's, cheaper one first — confirming whether automatic backups exist takes thirty seconds and may change how urgent the other one is. **HALF DONE IN S14: the dump exists.** `C:/Users/tomme/Backups/MichaelinMap/20260809-2055/` — all 8 tables, **1004 rows**, each as `.json` (fidelity) and `.sql` (`INSERT`s with `ON CONFLICT DO NOTHING`, ordered by FK dependency), plus a README carrying the restore steps and the number that proves a restore worked (`place_tags` with `source='curator'` = 345). **Verified by re-reading all 8 tables and deep-comparing field by field, not by counting rows** — equal counts with different content pass a count check and fail this one. **It is deliberately NOT in the project folder: the repo lives inside OneDrive**, and this dump carries Michael's entire list and his email, so writing it beside the repo would have published it by accident. **What it is not:** a logical export, not `pg_dump` — the MCP has no dump tool and `.env.local` holds no connection string. Enough in practice, because schema, RLS, RPCs and vocabulary all rebuild from the 11 migrations; **the judgment does not**, and that is what is saved. It does not cover `auth.users` (Michael's account and password — recreate through the dashboard). **WHAT REMAINS, thirty seconds of Edu's:** confirm in the dashboard whether this project has automatic backups at all. Nobody has checked, the free plan historically does not, and the answer decides how often this needs repeating. **A one-off backup ages** — this one was already stale the moment the curator confirmed the next tag |
| ✅ OP-03 | Version the project off this machine | Done in S04, though not where it was planned. The Git credential on this machine belongs to the `AntonioTavaresDevWork` account, which cannot see `AdminFeedpro/MichaelinMap` — hence the push answering "Repository not found". By Edu's decision the project now lives at `AntonioTavaresDevWork/MichaelinMap` (private), with the 9 commits and the founding history preserved |
