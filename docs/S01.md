Li tudo: PRD (17 seções), CLAUDE.md do produto, PLAN.md (9 fases), schema.sql, seed.sql, import-places.ts e o CSV master (511 linhas, validado coluna a coluna).

O que o material entrega

Guia de lugares curado por um crítico único (o "Michael"), público e read-only, escopo global mas 91% Austin. O moat é o julgamento: tier escasso (destination › experience › fair p/ restaurantes, cool › fair p/ bares), estrela cruzando tiers (22/511 = 4%), tags de propósito e não só de categoria, e uma camada de personalidade (Codes que transformam a UI em runtime, field reports absurdistas). Schema pronto, seed de 93 tags + 38 perguntas, CSV limpo, plano de 9 fases com exit test por fase.

Material bom. Validei os números do PRD contra o CSV e batem todos (511 linhas, 273 restaurantes, 91 bares, 22 starred, 42 não-visitados, 28 conflitos TIER+UNVISITED, 19 guias de origem, 0 Apple IDs duplicados, 0 coordenadas faltando).

---

Achados que mudam o plano

Sete coisas que encontrei e que não estão nos documentos:

┌─────┬───────────────────────────────────────────────────────────────┬───────────────────────────────────────────┐
│  #  │                            Achado                             │                  Impacto                  │
├─────┼───────────────────────────────────────────────────────────────┼───────────────────────────────────────────┤
│     │ As 511 places chegam sem tags. A coluna Tags do CSV tem só 5  │ Gargalo #1 do projeto. Filtragem facetada │
│ 1   │ valores distintos (Breakfast & brunch 54, Rooftop 14, Night   │  é a Fase 2 e o coração do produto — e    │
│     │ out 3, Vacation 2, Food truck 1). O vocabulário de 93 tags    │ não tem dado. Alguém precisa taggear 511  │
│     │ nasce 100% vazio                                              │ lugares à mão                             │
├─────┼───────────────────────────────────────────────────────────────┼───────────────────────────────────────────┤
│     │ codes vaza todos os códigos. A policy public reads active     │ Mata a feature que o PRD §4.1 chama de    │
│ 2   │ codes using (active = true) permite a qualquer visitante      │ mais importante                           │
│     │ fazer select * from codes e listar todo código secreto        │                                           │
├─────┼───────────────────────────────────────────────────────────────┼───────────────────────────────────────────┤
│     │ field_reports aceita insert livre. with check (true) permite  │ Viola a hard constraint #8 e a meta "zero │
│ 3   │ ao visitante gravar status='published' direto, furando o      │  free-text publicado sem revisão"         │
│     │ review-gate das 4 perguntas de texto livre                    │                                           │
├─────┼───────────────────────────────────────────────────────────────┼───────────────────────────────────────────┤
│     │ Qualquer conta autenticada é curador. auth.role() =           │                                           │
│ 4   │ 'authenticated' + signup aberto no Supabase = estranho cria   │ Falha de segurança real                   │
│     │ conta e escreve no guia                                       │                                           │
├─────┼───────────────────────────────────────────────────────────────┼───────────────────────────────────────────┤
│ 5   │ import-places.ts quebra em runtime. Faz upsert(onConflict:    │ Fase 0 não passa                          │
│     │ 'apple_id') mas apple_id não tem UNIQUE no schema             │                                           │
├─────┼───────────────────────────────────────────────────────────────┼───────────────────────────────────────────┤
│ 6   │ Seed não é idempotente. questions não tem constraint de       │ O PLAN exige idempotência no exit da Fase │
│     │ unicidade — rodar 2× duplica as 38 perguntas                  │  0                                        │
├─────┼───────────────────────────────────────────────────────────────┼───────────────────────────────────────────┤
│ 7   │ 4 lugares violam o cruzamento tier×tipo: Grocery com Fair(2)  │ Se eu travar a regra por constraint (que  │
│     │ e Destination(1), Bar com Experience(1), Outdoors com Fair(1) │ é o certo), essas 4 linhas são rejeitadas │
└─────┴───────────────────────────────────────────────────────────────┴───────────────────────────────────────────┘

Menores, mas registro: a view field_report_aggregates precisa de security_invoker = on senão bypassa RLS; Hype trap é citado no PRD §6.4 mas não existe nas 93 tags do seed; o PRD §9.10 descreve um primitivo Collection unificando codes/listas/shortlist que não existe no schema; place_tags tem SELECT público irrestrito (vaza IDs de lugares não publicados).

---

Onde o material colide com as regras deste projeto

O .claude/CLAUDE.md (framework Wise*) e o CLAUDE.md que veio do Claude Web se contradizem em quatro pontos. Minhas recomendações — preciso só do seu OK:

┌────────────────────────────┬──────────────────────┬─────────────────────────────────────────────────────────────┐
│        Regra Wise*         │    Michaelin Map     │                          Recomendo                          │
├────────────────────────────┼──────────────────────┼─────────────────────────────────────────────────────────────┤
│ company_id + RLS           │ Curador único,       │ Dispensar multi-tenant, registrar como ADR na Bíblia.       │
│ multi-tenant em toda       │ produto público      │ Forçar company_id aqui é cerimônia sem função               │
│ tabela                     │                      │                                                             │
├────────────────────────────┼──────────────────────┼─────────────────────────────────────────────────────────────┤
│ UI em Português BR,        │ Produto inteiro em   │ UI e conteúdo em inglês, formato en-US. Comentários de      │
│ formato 1.000,00,          │ inglês, Austin,      │ código seguem em inglês (já é a regra). Nossa conversa      │
│ DD/MM/YYYY, R$             │ $/$$$                │ segue em PT-BR                                              │
├────────────────────────────┼──────────────────────┼─────────────────────────────────────────────────────────────┤
│ Soft delete via deleted_at │ status = closed |    │ Aceitar status como o mecanismo — é mais expressivo aqui    │
│                            │ hidden               │                                                             │
├────────────────────────────┼──────────────────────┼─────────────────────────────────────────────────────────────┤
│ Mutação via RPC SECURITY   │ Escrita direta via   │ Manter a regra: rpc_submit_field_report e rpc_redeem_code   │
│ DEFINER                    │ client               │ resolvem os achados #2 e #3 de uma vez                      │
└────────────────────────────┴──────────────────────┴─────────────────────────────────────────────────────────────┘

E duas dependências novas exigem sua aprovação explícita (regra "nunca instalar dependência nova sem OK"): MapLibre GL (obrigatório — o Codes depende de troca de estilo de mapa em runtime, um embed do Google não faz isso) e Google Places API (hidratação de horários, telefone, faixa de preço — ~511 requests no one-shot, custo estimado abaixo de US$10).

---

Minhas dúvidas — o que preciso de você

Sobre o negócio e o processo:

1. Quem é o curador operacional? O produto fala de "Michael" (Michael's Top Faves) mas você é o dev. O Michael é cliente, sócio, ou é um projeto seu com persona? Isso define quem opera o admin, quem responde as 7 perguntas abertas do PRD §16, e quem vai taggear os 511 lugares.
2. Quem taggeia? O achado #1 é o caminho crítico. Sem tags, o produto é um mapa de pins bonito. São 511 lugares × ~5 facetas. Minha recomendação: usar os types da Google Places API para pré-popular cuisine e format automaticamente na hidratação (Fase 4), deixando pro curador só revisar e adicionar as facetas subjetivas — occasion, vibe, character. Isso transforma semanas em horas. Você topa essa abordagem, ou o curador quer atribuir tudo à mão por princípio?
3. Qual o prazo/ambição? Nove fases é MVP grande. Se existe uma data de demo, eu corto o escopo pra chegar lá — o PRD §4.1 deixa claro que o sucesso é "mostrar pra uma pessoa específica", o que sugere que Fases 0–3 + 7 (Codes) entregam o valor e as Fases 5, 6, 8, 9 podem esperar.

Sobre execução:

4. Import: script ou migration? O import-places.ts precisa da SERVICE_ROLE_KEY na sua máquina. Recomendo eu converter o CSV em uma migration de seed idempotente aplicada via MCP — fica versionada, não expõe service_role e resolve os achados #5 e #6 juntos. Confirma?
5. As 5 perguntas de curadoria do PRD que bloqueiam fases e ninguém além do curador responde: (a) destination está mesmo acima de experience? (b) cidades com 1 lugar só — mostrar como pares, agrupar as texanas em "Central Texas", ou suprimir? (c) os nomes públicos dos tiers ficam esses ou ele renomeia? (d) Hype trap é público ou só admin? (e) o link é indexável pelo Google ou não-listado? — Você consegue essas respostas, ou trabalho com defaults e marco como dívida no BACKLOG?

---

O que proponho fazer a seguir

Nesta sessão, sem tocar em código de produto: traduzir esse material para o formato Wise* — Bíblia preenchida (com os achados e as decisões acima incorporados), DOMAIN_QUESTIONS (o PRD tem material pra isso mas não no formato DQ), GANTT real com as 9 fases, BACKLOG com os 7 achados + as questões abertas, e STATUS corrigido (hoje ele mente: diz "Setup concluído" com o banco vazio).

Depois disso, F-00 = schema corrigido aplicado via migration + seed idempotente.

Me responde as 5 dúvidas (ou as que souber) e eu sigo.
